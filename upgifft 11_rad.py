import random
import turtle


Red = turtle.Turtle()
Blue = turtle.Turtle()

def initializing(color_of_turtle = str, size = int, speed_of_turtle = int, name = str, start_pos):
    name = name.turtle.Turtle()
    name.goto(start_pos)
    name.turtlesize(size)
    name.speed(speed_of_turtle)
    name.color(color_of_turtle)

initializing("red", 10, 0, Red, (2, 0))

turtle.done()

